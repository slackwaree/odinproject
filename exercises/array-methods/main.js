function camelize(upper) {
    let string = "background-color";
    let locateChar = string.indexOf("-");
    let replaceChar = locateChar.charAt();
    console.log(locateChar.toUpperCase());
}
camelize();